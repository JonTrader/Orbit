# Database (Neon / Postgres)
DATABASE_URL=
# Dedicated Neon branch for `npm test`. The suite drops and recreates the
# public schema on every run, so this must never be DATABASE_URL. The explicit
# confirmation is required by the test database safety guard.
DATABASE_URL_TEST=
DATABASE_URL_TEST_CONFIRMATION=dedicated-neon-branch
# Optional: point `npm run db:migrate` at a branch other than DATABASE_URL.
DRIZZLE_DATABASE_URL=

# Better Auth
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=

# OAuth (Google)
# Redirect URI: ${BETTER_AUTH_URL}/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# OAuth (Microsoft)
# Redirect URI: ${BETTER_AUTH_URL}/api/auth/callback/microsoft
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=

# Email (Resend)
RESEND_API_KEY=
EMAIL_FROM=

# Jobs (Inngest)
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=
